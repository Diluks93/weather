import { MainBackgroundDirective } from './main-background.directive';

describe('MainBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MainBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
